// registerElements.js
/*export const registerElements = () => {
  return new Promise(
    function( completionHandler ) {
      let topPanel = document.querySelector(".topPanel"),
        //contentPanel = document.querySelector(".contentPanel"),
        //fleetContainer = document.getElementById("animFleetContainer"),
        //helperText = fleetContainer.querySelector(".helperText"),
        headerTiles = gsap.utils.toArray(topPanel.querySelectorAll(".tlhTile")),
        tileLabels = gsap.utils.toArray(topPanel.querySelectorAll(".label")),
        tileChevrons = gsap.utils.toArray(topPanel.querySelectorAll(".iconContainer"));

      completionHandler()
  })
}*/

/*export const registerElements = () => {
    let topPanel = document.querySelector(".topPanel"),
    //contentPanel = document.querySelector(".contentPanel"),
    //fleetContainer = document.getElementById("animFleetContainer"),
    //helperText = fleetContainer.querySelector(".helperText"),
    headerTiles = gsap.utils.toArray(topPanel.querySelectorAll(".tlhTile")),
    tileLabels = gsap.utils.toArray(topPanel.querySelectorAll(".label")),
    tileChevrons = gsap.utils.toArray(topPanel.querySelectorAll(".iconContainer"));

    let elements = {
      topPanel: topPanel,
      headerTiles: headerTiles,
      tileLabels: tileLabels,
      tileChevrons: tileChevrons
    }

    return elements

  }*/
